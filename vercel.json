{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install && prisma generate && prisma migrate deploy",
  "crons": [
    {
      "path": "/api/cleanup",
      "schedule": "0 3 * * *"
    }
  ]
}
